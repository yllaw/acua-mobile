// Add your native dependencies here:

android {
  defaultConfig {  
    generatedDensities = []
    applicationId = "gov.lacounty.acua"
  }
  aaptOptions {  
    additionalParameters "--no-version-vectors"  
  }  
} 
